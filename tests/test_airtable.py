# commenting this until there's a safe way to run tests against airtable
""" def test_to_xarray():
    img_name = 'jrc_cos7-1b/em/fibsem-uint8'
    base = airt.get_airbase()
    imgModel = airt.query_image_by_name(img_name)
    datarray = airt.to_xarray(imgModel) """
